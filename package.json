{
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "gh-pages -d dist"
  },
  "gh-pages": {
    "src": [
      "dist/**/*"
    ]
  },
  "dependencies": {
    "express": "^4.18.2",
    "gh-pages": "^6.1.0",
    "three": "^0.153.0"
  }
}
